<!-- git --version
git config --global user.name "Shree"
git config --global user.email "22cs100@nandhaengg.org"
git config --global init.defaultbranch main
git init
git add .
git commit -m "New Project Created Using Java OOPs"
git log
git remote add origin https://github.com/Sree3008/Pizza-Bill-Generator-Application-Using-Java-OOPs-Concept.git
git push -u origin main -->

# commands used to push all into the remote repo